{
  "name": "eks",
  "version": "1.0.0",
  "description": "## Get iam user aws iam access key ``` terraform state pull | jq '.resources[] | select(.type == \"aws_iam_access_key\") | .instances[0].attributes' ```",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
